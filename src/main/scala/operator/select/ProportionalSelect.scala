package operator.select

import solution.Solution

class ProportionalSelect extends Select {
  override def apply(population: List[Solution], size: Int, withReplacement: Boolean = false): List[Solution] = ???
}
